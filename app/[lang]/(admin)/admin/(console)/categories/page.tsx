import AdminCategories from "@/components/admin/AdminCategories";

/** Auth enforced by admin console layout + proxy. */
export default function AdminCategoriesPage() {
  return <AdminCategories />;
}
